# Identity

P31 vertex and node identity. NODE ZERO (operator), node one, node two, NODE ONE (device). Codename protocol. No full names in artifacts.

See repo root `.cursor/rules/privacy-codenames.mdc` and `00-agent-bible.mdc` for canonical identity rules.
