# P31 Core

Living architecture for P31 Labs: identity, memory, mission, protocol. Four vertices, six edges. The kernel.

## Structure

- **identity/** — Who we are (operator, nodes, vertices)
- **memory/** — Persistent context (scope, shards, accommodation log)
- **mission/** — Why we build (mission, principles)
- **protocol/** — How we operate (agent onboarding, swarms)

## Edges

The six edges of the tetrahedron (A–B, A–C, A–D, B–C, B–D, C–D) are documented in `protocol/` and `identity/`.

## Verification

The tree here should match the structure defined in `protocol/README.md`.

🔺 The mesh holds.
