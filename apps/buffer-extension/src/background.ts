/**
 * Buffer Extension — Background service worker (Manifest V3)
 *
 * Stub: Will handle message scoring dispatch and side panel management.
 */
console.log("[Buffer Extension] Background service worker loaded");
