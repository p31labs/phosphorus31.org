/* P31 Shelter — R3F JSX types. React 19 uses react/jsx-runtime; we do not augment it
   to avoid replacing HTML intrinsics. JitterbugScene uses R3F elements; type-check
   for that file is relaxed until @react-three/fiber ships React 19 types. */
