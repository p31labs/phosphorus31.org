# Shelter (Buffer PWA) environment
VITE_SHELTER_URL=http://localhost:3001

# Tandem AI — configure in Settings (AI Provider) or use env as fallback
# Sovereign: set provider to Ollama in Settings; no key needed, runs on localhost
VITE_AI_PROVIDER=ollama
VITE_OLLAMA_URL=http://localhost:11434
VITE_DEEPSEEK_KEY=
VITE_DEEPSEEK_MODEL=deepseek-chat
VITE_AI_BASE_URL=https://api.deepseek.com
