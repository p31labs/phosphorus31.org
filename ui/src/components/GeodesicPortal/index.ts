/**
 * Geodesic Portal — quantum teleportation between two structures on the IVM.
 * Stub for full implementation: createPortal(fromId, toId), coherence cost/drain,
 * shimmer rings at source and destination, particle flow along geodesic.
 */

export type { GeodesicPortalState, CreatePortalPayload } from './types';
export { GeodesicPortalRing } from './GeodesicPortalRing';
