# Colyseus
COLYSEUS_PORT=2567
API_PORT=3001

# PostgreSQL (optional — Colyseus runs without DB)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=p31
DB_USER=p31
DB_PASSWORD=

# Redis (optional — in-memory driver used if not set)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth
JWT_SECRET=change-me-in-production
