/**
 * @p31labs/game-core — Phase transition (freeze/thaw) for bonding and mesh.
 * Consumed by Bonding UI; full logic can be adopted from Claude's p31-universe when merged.
 */

export { freeze, thaw, getPhase, resetPhase } from "./phase";
export type { PhaseState } from "./phase";
